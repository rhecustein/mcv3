name: domain
description: Domain layer containing entities, use cases, and repository interfaces
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # Core package
  core:
    path: ../core

  # Utilities
  equatable: ^2.0.5
  dartz: ^0.10.1

  # Code Generation
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code Generation
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1

  # Linting
  very_good_analysis: ^5.1.0

  # Testing
  mocktail: ^1.0.2
